# HotelinkingTest
backend:
#instalar dependencias con composer install
#correr migraciones con php artisan migrate
#seedear base de datos con php artisan db:seed --class=OfertsSeeder
#levantar el servidor con php artisan serve --host=localhost --port=8000

Frontend:
#instalar dependencias con npm i o npm install
#limpiar las coockies del navegador.
#levantar server con npm run dev.
#tener instalado php, xampp (base de datos mysql),composer y node.
